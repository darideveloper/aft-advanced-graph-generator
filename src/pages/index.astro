---
import BarChart from '../components/sections/BarChart.astro'
import '../styles/tailwind.css'

// Capturar parámetros de la URL (survey_id y participant_id)
const url = new URL(Astro.request.url)
const raw_data = url.searchParams.get('data')

const json_data = JSON.parse(raw_data ?? '{}')
console.log(json_data)
---

<html lang='es'>
  <head>
    <meta charset='utf-8' />
    <meta
      name='viewport'
      content='width=device-width, initial-scale=1'
    />
    <title>AFT Advanced Graph Generator</title>
  </head>
  <body>
    <main>
      <BarChart
        data={json_data}
      />
    </main>
  </body>
</html>
