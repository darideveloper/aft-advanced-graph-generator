import { defineConfig } from 'astro/config';

export default defineConfig({
  // Puedes agregar configuraciones de Vite o Astro si quieres, 
  // pero para Tailwind no necesitas el plugin @tailwindcss/vite
});
